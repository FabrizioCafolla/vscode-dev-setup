# Pofiles configuration
# [profile <name>]
# sso_session = <session_name>
# sso_account_id = <account_id>
# sso_role_name = <role_name>
# output = json

# SSO configuration
# [sso-session <name>]
# sso_start_url = <url>
# sso_region = <region>
# sso_registration_scopes = sso:account:access
